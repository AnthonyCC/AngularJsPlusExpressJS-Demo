extends layout

block content
  h1= title
  p Welcome to #{title}, testing by #{author}
  div Environment5: #{settings.env}
  div test subdiv
  script.
    var length = $('div').length;
    var text = '#{title}';
